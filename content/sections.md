---
# Site chrome copy. `label` is the word above the nav dots at the bottom of the
# screen; `eyebrow` is the small uppercase line at the top of each section.
# The hero eyebrow is built from `role` and `location` in profile.md.
hero:
  label: Intro

now:
  label: Now
  eyebrow: Current orbit

journey:
  label: Journey
  eyebrow: The journey so far

skills:
  label: Skills
  eyebrow: Star chart · skills

contact:
  label: Contact
  eyebrow: Send a signal
---
